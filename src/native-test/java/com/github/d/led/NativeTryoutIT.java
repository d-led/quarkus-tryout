package com.github.d.led;

import com.github.d.led.controllers.TryoutTest;
import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeTryoutIT extends TryoutTest {

    // Execute the same tests but in native mode.
}